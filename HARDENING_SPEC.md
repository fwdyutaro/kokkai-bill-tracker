# 法律案トラッカー 運用安全性改善仕様

## 1. 目的

本仕様は、法律案トラッカーの日次更新および利用者投稿の自動処理について、次の事故を防ぐための改修要件を定める。

- 収集元サイトの一時障害によって、正常な公開データが欠落・消失する
- `--only` や `--limit` を使った確認実行によって、全件データが少数件で上書きされる
- 情報提供・取り下げ依頼の処理に失敗したのに、Issue が「対応済み」として閉じられる
- 利用者が投稿したURLを自動取得することで、内部向けアドレスへのアクセスや過大なデータ取得が発生する
- パイプラインの途中で失敗しても、後続処理が続き、成功したように表示される

## 2. 対象範囲

### 対象

- `collect.py`
- `ingest_submission.py`
- `run_all.bat`
- `.github/workflows/update.yml`
- `.github/workflows/submit-ref.yml`
- `.github/workflows/remove-ref.yml`
- 上記に対する自動テスト

### 対象外

- 法案ステータスの判定基準そのもの
- 参考資料の関連度スコア算出方法
- `index.html` のデザイン変更
- 新しい収集元の追加

## 3. 基本方針

1. 更新に失敗した場合は、直前の正常な公開データを維持する。
2. 不完全なデータを正常データとして公開しない。
3. 確認用の部分実行と、本番用の全件実行を明確に区別する。
4. 外部入力は信用せず、検証を通過したものだけを取得・保存する。
5. 自動処理が成功したことを確認できた場合だけ、Issue を閉じる。
6. 失敗は終了コード、ログ、Issue コメントのいずれからも判別できるようにする。

## 4. 機能要件

### FR-1: 収集結果の安全な保存

#### 要件

`collect.py` は、収集結果を直接 `bills.json` と `data_collected.js` に書き込んではならない。最初に一時ファイルへ出力し、検証に合格した場合だけ正式ファイルと置き換える。

検証では最低限、次を確認する。

- 一覧ページから検出した法案数が1件以上である
- 詳細解析の成功率が95%以上である
- 全件実行で詳細解析に失敗した既存法案は、既存の `bills.json` にある同一 `id` のレコードで補完し、公開データから消失させない
- 各レコードに `id`、`no`、`title`、`status`、`refs` が存在する
- `id` が重複していない
- `refs` が配列である
- 全件実行時、既存データが存在する場合は、原則として既存件数の90%未満に減少していない
- 既存の会議録リンクが10件以上ある場合、会議録リンク数が既存比50%未満へ減少していない

法案件数または会議録リンク数が大幅に減少する正当な会期変更では、後述の `--allow-large-decrease` を明示的に指定する。

一時ファイルから正式ファイルへの置換は、同一ファイルシステム上の原子的な置換を使用する。片方だけが新しくなることを防ぐため、`bills.json` を正本として保存した後、同じ検証済みデータから `data_collected.js` を生成する。

#### CLI変更

- `--output PATH`: JSONの出力先を指定する。既定値は `bills.json`
- `--allow-partial-output`: `--only` または `--limit` の結果を明示した出力先へ保存することを許可する
- `--allow-large-decrease`: 既存法案件数または会議録リンク数の大幅な減少を明示的に許可する

`--only` または `--limit` が指定され、`--output` が既定値のままの場合は、処理を開始せず終了コード2で終了する。`--allow-partial-output` だけでは既定の本番ファイルへの上書きを許可しない。

#### 失敗時の動作

- 正式な公開ファイルを変更しない
- 一時ファイルを公開用ファイルとして残さない
- 標準エラーに、検出件数、成功件数、失敗した法案番号、失敗理由を出力する
- 終了コード1を返す。CLIの使い方が不正な場合は終了コード2を返す

#### 受け入れ条件

- 100件中10件の詳細取得が失敗した場合、既存の公開データは変更されない
- 既存100件中2件の詳細取得が失敗した場合、その2件は既存レコードで補完され、100件の出力が検証を通過し保存される
- 新規法案の詳細取得に失敗して既存レコードで補完できない場合は、その旨を警告ログに明示する
- `python collect.py --limit 3` は、本番ファイルを変更せず終了コード2になる
- `python collect.py --limit 3 --output tmp/sample.json --allow-partial-output` は成功する
- 保存処理中に例外が発生しても、既存の `bills.json` は有効なJSONのまま残る
- 国会会議録APIの全面障害などで会議録リンクが既存比50%未満になる場合、公開ファイルを変更しない

### FR-2: 利用者投稿URLの安全確認

#### 要件

`ingest_submission.py` は、URLへアクセスする前と、各リダイレクト先へアクセスする前に安全性を検証する。

許可するURLは次をすべて満たすものに限る。

- スキームが `https` または `http`
- ホスト名が存在する
- ユーザー名・パスワードをURLに含まない
- ポートは80または443。明示されない場合も許可する
- DNS解決結果がすべてグローバル到達可能なIPアドレスである

DNS rebinding対策として、DNS解決は検証時に1回だけ行い、その解決済みIPを各HTTP接続の完了まで固定する。HTTPの `Host` ヘッダーとTLSのSNI・証明書検証には元のホスト名を維持する。
実装は `socket.getaddrinfo` の一時的な固定を伴うため、同一プロセス内の取得をロックで直列化し、
`ingest_submission.py` は単一スレッドのCLIとして実行する。環境変数由来のHTTPプロキシは使用しない。

次の宛先は拒否する。

- `localhost` および `.localhost`
- ループバック、プライベート、リンクローカル、予約済み、マルチキャスト、未指定アドレス
- IPv4とIPv6の上記相当範囲
- 検証時に得たIP以外の接続先
- 80、443以外のポート

#### HTTP取得制限

- 接続タイムアウト5秒、読み取りタイムアウト10秒
- リダイレクトは最大3回
- リダイレクトのたびにURLとIPを再検証する
- 応答本文は最大2 MiBまで読み込む
- `Content-Type` はHTMLまたはXHTMLのみを自動解析する
- HTTP 200以外は正常なメタデータ取得として扱わない
- TLS証明書検証を無効化しない
- Cookieや認証情報を次のホストへ引き継がない

安全性検査は `validate_public_url()`、取得は `fetch_public_html()` のように分離し、単体テスト可能にする。

PDFなどHTML/XHTML以外の投稿URLは自動解析せず、`manual_review` として人による書誌確認へ回す。

#### 任意の許可ドメイン運用

環境変数 `SUBMISSION_ALLOWED_DOMAINS` が設定されている場合は、指定されたドメインおよびそのサブドメインだけを許可する。未設定の場合は、安全性検査に合格した公開インターネット上のホストを許可する。

#### 失敗時の動作

- URLへアクセスせず、または取得を直ちに中止する
- `rec.json` を生成しない
- 利用者向けには「URLを安全に取得できなかったため手動確認が必要」と表示する
- ログには、拒否理由を含める。ただし認証情報やURLクエリ中の機密情報は出力しない
- 安全な取得ができない場合は終了コード1、入力書式不正は終了コード2、予期しない内部障害は終了コード3を返す
- GitHub Actionsは終了コード1・2を `manual_review` に変換して正常に状態処理し、終了コード3以上は `failed` としてワークフローを失敗させる

#### 受け入れ条件

- `http://127.0.0.1/`、`http://localhost/`、`http://169.254.169.254/` を拒否する
- RFC1918のプライベートIPv4と、IPv6のループバック・リンクローカルを拒否する
- 公開URLからプライベートIPへのリダイレクトを拒否する
- 2 MiBを超える応答を途中で中止する
- 通常の公開HTMLページからタイトルと説明を取得できる

### FR-3: 情報提供Issueの状態管理

#### 要件

`.github/workflows/submit-ref.yml` は、処理結果を次の状態で扱う。データ準備完了と公開完了を区別する。

- `published-ready`: レコード生成とJSON反映が完了し、コミット・push可能
- `published`: `published-ready` の後、コミットとpushが成功した最終状態
- `manual_review`: 入力解釈、URL取得、メタデータ取得などに失敗し、人による確認が必要
- `failed`: Actions、Git、JSON処理などの予期しない障害

`published` の場合だけIssueを閉じる。

`manual_review` の場合は次を行う。

- Issueを閉じない
- `approved` ラベルを外す
- `needs-review` ラベルを維持または追加する
- 手動対応が必要な理由をコメントする
- ワークフロー自体は、想定された要確認状態として正常終了してよい

`failed` の場合はIssueを閉じず、ワークフローを失敗させる。

#### 実装条件

- レコード生成ステップは `$GITHUB_OUTPUT` へ `result` を出力する
- コミット＆クローズステップには `if: steps.<id>.outputs.result == 'published-ready'` 相当の条件を付ける
- `|| true` で予期しない失敗を隠さない
- 投稿受付時の `ingest` ジョブにも終了コードの状態変換を適用し、終了コード1・2だけを手動確認として扱う
- `git push` 成功後にだけIssueを閉じる
- 差分が存在しない重複投稿は、既掲載である旨をコメントして閉じてもよい。ただし「新規掲載した」とは表示しない

#### 受け入れ条件

- 法案番号を解釈できない場合、Issueは開いたままになる
- 危険なURLの場合、Issueは開いたままになり、理由がコメントされる
- `rec.json` が生成されない場合、コミット処理とクローズ処理は実行されない
- `git push` に失敗した場合、Issueは閉じられない
- 正常掲載時のみ「掲載しました」とコメントして閉じられる

### FR-4: 取り下げIssueの状態管理

#### 要件

`.github/workflows/remove-ref.yml` にもFR-3と同じ状態管理を適用する。

取り下げ対象について、次を確認する。

- 法案番号が既存の法案に存在する
- URLがその法案の `refs` に実際に存在する
- `suppressions.json` への追加後、対象URLが `bills.json` から除外された

対象が存在しない場合はIssueを閉じず、`needs-review` として理由をコメントする。既に除外済みの場合は、その旨をコメントし、重複依頼として閉じてもよい。

取り下げURLは安全側に倒して完全一致で照合する。末尾スラッシュ、クエリ、URLエンコードなどが異なる場合は自動正規化せず `manual_review` とする。

#### 受け入れ条件

- 法案番号またはURLを解釈できない場合、Issueは閉じられない
- 存在しないURLの取り下げ依頼は、自動的に「除外済み」と扱われない
- JSONへの追加だけでなく、公開データから消えたことを確認してからIssueを閉じる
- `git push` 失敗時はIssueを閉じない

### FR-5: ローカル日次パイプラインの失敗停止

#### 要件

`run_all.bat` は、必須工程が失敗した時点で直ちに停止し、非ゼロ終了コードを返す。

必須工程は次のとおり。

1. `collect.py`
2. `crawl.py`
3. `match_refs.py`
4. `merge_submissions.py`
5. `apply_suppressions.py`
6. `tag.py`

`linkcheck.py` は検査工程とし、死リンクを検出した場合の扱いを次から設定可能にする。

- 既定: 警告を出すが、データ生成成功として終了する
- `STRICT_LINKCHECK=1`: 非ゼロ終了として扱う

#### 出力

- 成功時のみ `完了` と表示する
- 失敗時は、失敗した工程名と終了コードを表示する
- 呼び出し元のタスクスケジューラが検知できる終了コードを返す

#### 受け入れ条件

- `collect.py` が失敗した場合、`crawl.py` は実行されない
- `match_refs.py` が失敗した場合、タグ付与や完了表示は行われない
- 全必須工程の成功時だけ終了コード0になる

### FR-6: GitHub Actionsの同時更新制御

#### 要件

日次更新、情報提供、取り下げ処理が同時に同じブランチへpushしないよう、同一の `concurrency.group` を設定する。

- グループ名は全更新ワークフローで共通にする
- 実行中のデータ更新を途中キャンセルしないため、`cancel-in-progress: false` とする
- push直前にリモートの更新を取り込み、競合時はIssueを閉じず失敗させる

GitHub Actionsのconcurrencyでは待機runが1件に制限され、新しいrunにより古い待機runがキャンセルされる場合がある。キャンセルされた投稿・取り下げIssueは開いたまま維持し、メンテナが対象runを手動で再実行する運用とする。

#### 受け入れ条件

- 日次更新中に投稿が承認されても、2つのpushが並行実行されない
- 競合を解決できない場合、公開済みと誤表示しない

### FR-7: CI依存関係の一貫性

#### 要件

GitHub Actionsは、手書きの個別パッケージ一覧ではなく、CI用に明示した依存ファイルからインストールする。

最低限、次を満たす。

- `ANTHROPIC_API_KEY` を利用する経路では `anthropic` がインストールされている
- CIで利用しない重量依存は別の追加依存グループに分離できる
- 主要依存のバージョン上限またはロックを設け、突然の破壊的変更を避ける

推奨構成例:

- `requirements-core.txt`: 日次収集とIssue処理に必要なもの
- `requirements-semantic.txt`: `sentence-transformers` など意味検索用
- `requirements-dev.txt`: pytest、静的検査など開発用

## 5. テスト要件

### 5.1 単体テスト

`pytest` を使用し、外部通信はすべてモックする。

最低限、次をテストする。

- 和暦からISO日付への変換
- 法案ステータス判定
- 部分実行時の上書き拒否
- 収集成功率と件数減少の検証
- JSONの必須フィールドとID重複検査
- URLのスキーム、ポート、IPアドレス検査
- リダイレクト先の再検査
- 応答サイズ上限
- Issue本文からの法案番号・URL抽出
- 情報提供と取り下げの `published-ready/published/manual_review/failed` 判定
- 会議録リンク数の大幅減少検査

### 5.2 結合テスト

固定HTMLフィクスチャを使用し、次を検証する。

- 一覧HTMLと詳細HTMLから期待する法案JSONを生成できる
- 1件だけ詳細取得に失敗しても、設定した成功率内なら出力できる
- 成功率を下回る場合は既存ファイルを維持する
- `bills.json` と `data_collected.js` が同一データを表す
- suppression適用後に指定URLが消える
- submission反映後に指定URLが1回だけ追加される

### 5.3 GitHub Actions検証

可能であればワークフローの処理本体をPythonまたはシェルスクリプトへ切り出し、ローカルテスト可能にする。少なくともYAMLの構文検査と、各結果状態に対する後続ステップ条件をレビュー対象とする。

## 6. ログおよび監視

各日次実行で、次の集計を出力する。

- 一覧で検出した法案数
- 詳細取得の成功数・失敗数・成功率
- 最終出力件数
- 前回からの増減数
- 参考リンクの追加数・削除数
- suppressionとsubmissionの反映数
- 公開ファイルを更新したか、既存データを維持したか

ログにはAPIキー、GitHubトークン、URL内の認証情報を出力しない。

## 7. 後方互換性

- `bills.json` の既存データ形式は変更しない
- `data_collected.js` の `window.BILLS = ...;` 形式を維持する
- `index.html` を変更せず表示できること
- `submissions.json` と `suppressions.json` の既存レコードを引き続き読み込めること
- 新しいCLI保護により、危険な部分実行だけは従来と異なり明示的に失敗させる

## 8. 実装順序

### 優先度1: 公開データ保護

1. `collect.py` の検証と原子的保存
2. 部分実行時の本番上書き禁止
3. `run_all.bat` の失敗停止

### 優先度2: 投稿処理の安全化

1. URL安全性検査と取得上限
2. 情報提供Issueの状態管理
3. 取り下げIssueの状態管理

### 優先度3: 運用安定化

1. Actionsの同時更新制御
2. CI依存関係の整理
3. テストとログ集計

## 9. 完了条件

次をすべて満たした時点で、本仕様の改修を完了とする。

- 本仕様の受け入れ条件を自動テストで確認できる
- 全テストがGitHub Actions上で成功する
- 収集元の一部障害を模擬しても既存公開データが失われない
- 危険なURLの取得をテストで拒否できる
- 自動処理失敗時にIssueが閉じられない
- 正常処理時だけデータをコミットし、Issueを閉じる
- ローカル日次処理が途中失敗を正しく終了コードで通知する
- 既存の105法案・744参考リンクを読み込んだ状態で、表示互換性に問題がない
