# 閉会対応・複数会期対応 レビューコメント

## 総合評価

**条件付き承認（修正後に再レビュー）**

第221回国会の閉会を反映するステータス判定、会期単位のデータ保存、会期セレクタ、
非公式サイトとしての注意書き、閉会中のCI実行判定は、主要部分が仕様どおり実装されている。

一方、次会期への完全自動移行と、参考情報の投稿・取り下げにおける会期識別には
データ整合性上の問題が残る。第222回国会など次の会期を追加する前に修正することを推奨する。

## 仕様別評価

| ID | 仕様 | 評価 | レビューコメント |
|---|---|---|---|
| A/C | 複数会期対応 | 本体は合格 | `sessions.yaml` / `sessions.py`による会期管理、指定会期だけの差し替え、他会期の保持、安全ガードの同一会期内比較を確認した。投稿・取り下げ処理には会期識別の不足が残る。 |
| B | 審査未了の判定 | 合格 | 明示的な「未了」に加え、閉会済み会期で決着のない法案を審査未了とする判定を確認した。閉会後7日間の猶予と、成立・継続審査・廃案を優先する判定順序も妥当。 |
| D | 会期セレクタ | 合格 | 最新会期の既定選択、「すべての会期」、6枚のダッシュボード、閉会注記、会期変更時の再集計を確認した。 |
| E | 表示文言 | 合格 | 「プロトタイプ」を廃止し、個人運営・非公式・一次資料での確認が必要である旨をタイトル、ヘッダー、フッター、READMEへ反映している。 |
| F | 閉会中のCI | 一部未達 | 閉会中の金曜日に`run=false`、土曜日に週次実行となる判定は正しい。ただし、次会期の収集対象はActions内の固定`DIET`に依存しており、`sessions.yaml`の更新だけでは完全移行できない。 |

## 修正必須コメント

### [P1] Actionsの収集対象会期を会期メタデータから取得する

**対象:** `.github/workflows/update.yml`

CIの実行頻度は`sessions.py`が判定する一方、収集対象は次の固定値に依存している。

```yaml
env:
  DIET: "221"
```

新しい開会中会期を`sessions.yaml`へ追加すると日次実行には戻るが、`DIET`を更新し忘れた場合は
第221回国会を再収集し続ける。この動作は「会期再開時は`sessions.yaml`の更新だけで自動的に
日次へ戻る」という仕様を完全には満たさない。

**推奨修正:**

- Actionsでも`python sessions.py latest`から対象回次を取得する。
- 固定の`env.DIET`を廃止する。
- 対象回次を取得できない場合は、既定値へフォールバックせず非ゼロで停止する。
- CIテストで、次会期追加時に新しい回次が収集対象になることを確認する。

### [P1] 参考情報の投稿に`bill_id`と会期を保持する

**対象:**

- `index.html`
- `ingest_submission.py`
- `merge_submissions.py`
- `.github/ISSUE_TEMPLATE/submit-ref.yml`
- `.github/workflows/submit-ref.yml`

複数会期では「閣法 第31号」などの法案番号が重複する。現在の投稿経路は主に法案番号だけを渡し、
取込時と掲載時で異なる会期の同番号法案を選ぶ可能性がある。

特に、不明な`bill_id`が指定された場合でも法案番号による最新会期へのフォールバックが行われるため、
過去会期向けの参考情報が新会期の別法案へ掲載され得る。

**推奨修正:**

- 詳細画面からIssueを作る際に`bill_id`と会期を渡す。
- 取込レコードと`submissions.json`に`bill_id`を保存する。
- `bill_id`が指定されているのに該当法案がない場合は、番号へフォールバックせず
  `manual_review`にする。
- 法案番号だけで複数候補がある旧形式の投稿も`manual_review`にする。
- 過去会期からの投稿が同じ会期へ掲載される結合テストを追加する。

### [P1] 取り下げ対象を`bill_id`で限定する

**対象:**

- `apply_suppressions.py`
- `issue_workflow.py`
- `.github/ISSUE_TEMPLATE/remove-ref.yml`
- `.github/workflows/remove-ref.yml`

現在の取り下げ処理は`(bill_no, URL)`を照合キーとして全会期へ適用する。
同じ法案番号とURLが複数会期に存在すると、1件の取り下げ依頼で複数会期の参考リンクが
削除される。

**推奨修正:**

- 取り下げIssueと`suppressions.json`に`bill_id`を保存する。
- 除外処理は原則として`(bill_id, URL)`で照合する。
- `bill_id`を持たない旧形式は、対象が一意に決まる場合だけ適用する。
- 複数会期に同番号・同URLがある場合の回帰テストを追加する。

### [P1] 会期日付を厳格に検証する

**対象:** `sessions.py`

不正な`from`または`to`が`None`へ変換されるため、閉会日を誤記すると、その会期が開会中として
扱われ続ける可能性がある。これにより、CIの実行頻度と審査未了判定の両方が誤る。

**推奨修正:**

- `from`を必須のISO日付として検証する。
- `to`は`null`または有効なISO日付だけを許可する。
- `to < from`を拒否する。
- 同じ回次の重複登録を拒否する。
- 不正な会期メタデータではパイプラインを停止する。
- 不正日付、必須項目欠落、回次重複のテストを追加する。

## 確認済み事項

- 第221回国会の会期は、2026年2月18日から2026年7月25日までの特別会として登録されている。
- 現在の収録件数は123件。
- ステータス内訳は、成立78件、継続審査21件、審査未了20件、廃案4件。
- 法案IDの重複は0件。
- 必須フィールドの欠落は0件。
- テスト92件はすべて成功した。
- UTC月曜から土曜の16:01をJST火曜から日曜の01:01として扱うcron換算は正しい。
- 閉会中の金曜日に`run=false`、土曜日に`run=true`となる判定を確認した。
- 公布済み、両院可決、継続審査、明示的な未了、否決・廃案、閉会推定の優先順位は妥当。

## 再レビューの受け入れ条件

- [ ] Actionsの対象回次が`sessions.yaml`から一意に決まる。
- [ ] 第222回国会を追加したテストで、第221回のデータを保持しつつ第222回だけを更新できる。
- [ ] 投稿Issueから掲載まで`bill_id`が保持される。
- [ ] 不明または曖昧な`bill_id`・法案番号は自動掲載されない。
- [ ] 取り下げが指定された会期・法案だけに適用される。
- [ ] 不正な会期日付でCIが停止する。
- [ ] 既存テストと追加テストがすべて成功する。

## レビュー結論

第221回国会の閉会を表示する現在の用途には利用可能である。

ただし、複数会期を長期運用する仕組みとしては、会期の自動選択、投稿、取り下げの3経路で
同じ`bill_id`を一貫して使用する必要がある。上記のP1項目を修正後、次会期追加前に再レビューする。

---

## 再レビュー実結果（2026-07-31）

- [x] `approve_submission` は既知 `bill_id` と議案番号の一致、または一意なlegacy議案番号を検証し、未知・曖昧な入力を `manual_review` として停止する。
- [x] `bill_id`指定・legacy指定の抑制を独立に適用し、対象外会期へ波及させない。
- [x] `parse_issue` / `parse_removal` の旧2要素APIを維持し、詳細処理は新しい `*_details` 関数へ分離した。
- [x] 会期メタデータの欠落・不正時に日本語警告を出して非0終了する。
- [x] 全pytest 105件、YAML parse、py_compile、diff --checkを実行済み。

## 最終実装結果（2026-07-31）

元の受け入れ条件7項目の対応状況:

- [x] P1-1: Actions/run_allの対象会期を`sessions.py latest`から動的取得（取得失敗時は停止）。
- [x] P1-2: `bill_id`をIssueテンプレート、画面、取り込み、承認、mergeまで伝搬。
- [x] P1-3: 抑制を`(bill_id, URL)`または一意legacy議案番号に限定。
- [x] P1-4: sessions metadataのfrom/to、順序、重複、欠落、壊れたYAMLを厳格検証。
- [x] P1-5: URL抽出、SSRF/DNS pin、曖昧議案番号のmanual reviewを検証。
- [x] P1-6: unknown/mismatch bill_id、legacy ambiguity、重複submissionを安全停止。
- [x] P1-7: 全pytest 106件、YAML parse、py_compile、git diff --checkを実行済み。

# 実装側からの返答（2026-07-31）

## 指摘の検証結果

P1の4件すべてを現行コードで確認した。**いずれも指摘のとおりであり、反論はない。**

| 指摘 | 検証 | 根拠 |
|---|---|---|
| P1-1 Actionsの会期が固定 | 該当 | `.github/workflows/update.yml:26` に `DIET: "221"` が残存。実行頻度は`sessions.py`が判定する一方、収集対象は手動更新に依存する。 |
| P1-2 投稿に`bill_id`が無い | 該当 | `index.html`の投稿リンクは`&bill=<議案番号>`のみを送出。`ingest_submission.py`・`issue_workflow.py`に`bill_id`の記述はない。 |
| P1-3 取り下げが全会期に波及 | 該当 | `apply_suppressions.py:32,38` が`(bill_no, url)`で照合しており、会期を問わず一致すれば削除される。 |
| P1-4 会期日付の検証が緩い | 該当 | `sessions._parse_date("2026-13-99")` が `None` を返すことを実行確認。閉会日を誤記すると開会中扱いのままとなる。 |

### P1-2 について補足（実装側の見落とし）

`merge_submissions.py:58` は `by_id.get(s.get("bill_id")) or by_no.get(s.get("bill_no"))` と
`bill_id`を優先する受け皿を持つが、**上流のどの経路も`bill_id`を生成していない**。
したがって現状は常に議案番号へフォールバックしており、この防御は機能していない。
実データの`submissions.json`も`bill_no`のみを保持している。

実装時に「会期衝突への対処済み」と整理したが、これは受け皿の存在だけを見た誤った評価であった。
指摘のとおり、Issueテンプレート・フロント・取込・掲載の全経路を揃えなければ意味をなさない。

## 現時点の影響範囲

指摘の妥当性を認めた上で、緊急度の判断材料として現状を記録する。

- 収録データは`221回`の単一会期のみ（123件）であり、議案番号の重複は構造上まだ発生しない。
- `suppressions.json`は空。`submissions.json`は1件（221回のみ）。

したがって **P1-2・P1-3は次会期を追加した時点で顕在化する潜在不具合**であり、
「現在の用途には利用可能、次会期追加前に修正」というレビュー結論と認識が一致する。

一方 **P1-1・P1-4は単一会期でも影響し得る**。
特にP1-1は、次会期召集時に`DIET`の更新を忘れると第221回を再収集し続け、
新会期が現れないまま正常終了するため、失敗に気づきにくい。

## 対応順序（提案）

1. **P1-4** `sessions.py`の日付厳格化。独立性が高く影響範囲が小さい。他の判定の前提となるため最初に入れる。
2. **P1-1** 収集対象を`sessions.py latest`から取得し、取得失敗時はフォールバックせず停止。次会期召集前に必須。
3. **P1-2 / P1-3** `bill_id`の一貫伝搬。Issueテンプレート・フロント・取込・掲載・取り下げの5経路に及ぶため最も重い。**次会期のデータを追加する前**を期限とする。

## 再レビュー時の確認事項

受け入れ条件のうち次の1件は、既存テストで充足済みと考えられる。重複作業を避けるため申し添える。

- [x] 「第222回国会を追加したテストで、第221回のデータを保持しつつ第222回だけを更新できる」
  → `tests/test_collect_merge.py::test_new_diet_run_keeps_previous_diet_records`
  （既存221回100件＋新規222回10件＝110件、会期別内訳まで検証済み）

同ファイルには安全ガードの会期スコープ（`test_decrease_guard_compares_within_the_same_diet`、
`test_new_diet_with_few_bills_does_not_trip_the_guard`）と未登録会期の警告
（`test_unregistered_diet_warns_but_continues`）のテストも含まれる。

残る受け入れ条件6件は未達であり、上記の対応順序で解消する。
